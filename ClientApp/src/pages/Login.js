import React, { Component } from "react";
import Card from "../components/ui/Card";

export default class Login extends Component {
  render() {
    return (
      <div>
        <Card> Login / Sign up</Card>
      </div>
    );
  }
}
